import 'package:flutter/material.dart';

class Postblogpage extends StatelessWidget {
  const Postblogpage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Post Blog Page"),
      ),
    );
  }
}
